services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/__pycache__
      - /app/.venv
      - saves_data:/app/saves
    environment:
      - FLASK_DEBUG=${FLASK_DEBUG:-1}

volumes:
  saves_data:
